<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>disable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <AssemblyName>ModelGen</AssemblyName>
    <RootNamespace>Mahou.Tools</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <!-- Reuse the exact runtime model code so the on-disk format can never drift. -->
    <Compile Include="..\..\Mahou\Classes\NgramScorer.cs" />
  </ItemGroup>
</Project>
